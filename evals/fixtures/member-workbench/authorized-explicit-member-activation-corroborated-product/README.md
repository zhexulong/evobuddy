# Corroborated Explicit Product Workbench Fixture

This retained Workbench eval fixture is generated from the path-normalized retained product proof root `../explicit-member-activation/authorized-explicit-member-activation-corroborated-product/`.

It locks the UI projection for the reviewed explicit product boundary:

- first-level Workbench views (`overview.txt`, `expert.txt`, `task.txt`) do not expose proof taxonomy labels such as `authorized-explicit-member-activation`, `authorized-natural-native-spawn`, `nativeSpawnPass`, `PRODUCT PASS`, or `MECHANISM PASS`;
- the member result body may still contain the canary `CTREE-SURVIVE-authorized-explicit-member-activation`;
- retained fixture input renders `Run kind: Retained run`, not `Live run`;
- trace detail carries proof/evidence information such as observed parent call and aligned digest state.

`Live run` is reserved for fresh/product observed roots outside retained fixture paths. This retained fixture is regression evidence only.
